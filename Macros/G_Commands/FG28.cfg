[gcode_macro FG28]
# rename_existing: G28_BASE klicky already uses homing_override
gcode:
    # {% set test = ((params.X ~ params.Y ~ params.Z) | default("X Y Z")).upper().split() %}

    # {% set axes = (rawparams | string or "X Y Z") %}
    # {% set axes = params.AXES | default("X Y Z") | string %}
    # _DEBUG MSG="{axes}"
    {% set axis_pos = { "X": "X" ~ (printer.toolhead.axis_maximum.x - 10),
                        "Y": "Y" ~ (printer.toolhead.axis_maximum.y - 10),
                        "Z": "X208.2 Y" ~ printer.toolhead.axis_maximum.y ~ " Z10" } %}

    # {% for axis in axes.upper().split() %}
    #     _DEBUG MSG="{axis}"
    # {% endfor %}

    {% for axis in (params.AXES | string or "X Y Z").upper().split() %}
        {% if (axis | lower) in printer.toolhead.homed_axes %}
            G1 {axis_pos[axis]} F9000
            G28 {axis}
        {% else %}
            CG28
        {% endif %}
    {% endfor %}

# [gcode_macro ATEST]
# gcode:
#     {% set axes_list = (rawparams | string).split() %}
#     {% if axes_list|length == 0 %}
#         {% set axes_list = ["X","Y","Z"] %}
#     {% endif %}

#     {% for axis in axes_list %}
#         _DEBUG MSG={axis}
#     {% endfor %}