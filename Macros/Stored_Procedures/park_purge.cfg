[gcode_macro PARKPURGE]
gcode:
    CG28
    SAVE_GCODE_STATE NAME=PARKPURGE
    G90
    G1 X{bucket_start + (bucket_left_width / (2 - bucket_pos)) + (bucket_pos * bucket_gap) + (bucket_pos * (bucket_right_width / 2))} F6000
    RESTORE_GCODE_STATE NAME=PARKPURGE