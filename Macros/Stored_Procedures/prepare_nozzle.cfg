[gcode_macro PREPARE_NOZZLE]
gcode:
    {% set EXTRUDER_TEMP = params.EXTRUDER | default(0) | float %}

    CG28

    {% if EXTRUDER_TEMP > 0 %}
        _status_heating
        M109 S{EXTRUDER_TEMP}
    {% endif %}

    _status_cleaning
    clean_nozzle

    PARKPURGE
    _status_off