[idle_timeout]
timeout: 1200
gcode:
    _CANCEL_ACTIVE_LIGHTSWITCH_OFF
    LIGHTSWITCH_OFF
    {% if printer["gcode_macro POWER_OFF"].poweroff == 1 %}
        POWER_OFF_SAFE DURATION=600
    {% endif %}