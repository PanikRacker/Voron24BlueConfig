[idle_timeout]
timeout: 1200
gcode:
    NEVERMORE_OFF
    LIGHTSWITCH_OFF DELAY=120
    POWER_OFF DELAY=600
    UPDATE_DELAYED_GCODE ID=IDLE_TIMEOUT_POWER_OFF DURATION=10800

[delayed_gcode IDLE_TIMEOUT_POWER_OFF]
gcode:
    POWER_OFF FORCE=True
